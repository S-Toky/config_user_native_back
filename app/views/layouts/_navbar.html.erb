<nav class="navbar navbar-expand-lg navbar-light bg-secondary">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Navbar</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="<%= root_path %>">Home</a>
        </li>
        <% if !user_signed_in? %>
        <li class="nav-item">
          <a class="nav-link" href="<%= new_user_path %>">s'inscrire</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="<%= new_session_path %>">se connecter</a>
        </li>
        <% else %>
        <li class="nav-item">
          <a class="nav-link" href="/profil">profil</a>
        </li>
        <li class="nav-item">
          <%= link_to "Se deconnecter", session_path(current_user.id), method: :delete , class: "nav-link"%>
        </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>